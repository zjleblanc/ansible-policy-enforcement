apiVersion: v1
kind: Service
metadata:
  name: "{{ opa_deployment_name }}"
  namespace: "{{ opa_namespace }}"
spec:
  selector:
    app: "{{ opa_deployment_name }}"
  ports:
    - name: https
      port: 8181
      targetPort: 8181
  type: ClusterIP 